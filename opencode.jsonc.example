{
  "$schema": "https://opencode.ai/config.json",
  // Este arquivo é opcional.
  // Os modelos principais já ficam definidos no frontmatter dos agentes.
  // Use este arquivo para configurações globais do projeto, MCPs, providers ou permissões.
  // Ajuste conforme a versão atual do OpenCode e sua documentação oficial.
}
